# HeyNoww Client Reports

This repository hosts simple static HeyNoww client reports for GitHub Pages.

Reports are plain HTML files with embedded CSS and no package dependencies.

Current report:

- Gavin: `gavin/index.html`
